//! `kotlin.*` numeric types: `Int`, `Long`, `Short`, `Byte`, unsigned variants,
//! `Float`, `Double`, `Math` / `kotlin.math.*`. The Zig intrinsics live in
//! `implementations`; symbol entries live in the generated registry.
