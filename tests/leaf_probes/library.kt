// Probe for the wide library leaf pack: pulls the kotlinx surfaces the
// census suites exercise into one image so `klio transpile` (leaves
// mode) walks their bodies. Never executed for its output.
import kotlinx.datetime.*

fun main() {
    println(LocalDate.fromEpochDays(0).toEpochDays())
}
